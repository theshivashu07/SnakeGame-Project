So, to run this code, must is we already have python setup,and also must that we do install one settup is called "curses".So, to install this package you go on command prompt,and just write that command, and do enter it...|......>> pip install windows-cursesAfter this successfully installed.Then you go on the same location on command prompt,and then open this file, lust like that...|......>> snakegame.pySo, now your game will working properly. But you not trying to do this code run on your editor/IDLE,because if you run there so it gives many errors...!!